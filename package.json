{
  "name": "v-tanks",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:pages": "tsc --noEmit && vite build --base=/v-tanks/",
    "preview": "vite preview",
    "render:tanks": "node tools/render-tanks.mjs",
    "test": "npm run build && node --experimental-strip-types --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2",
    "vite": "8.1.5"
  },
  "type": "module"
}
